'use strict';

/**
 * global-warming-potential service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::global-warming-potential.global-warming-potential');
