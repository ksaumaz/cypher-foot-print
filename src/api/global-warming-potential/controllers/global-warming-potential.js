'use strict';

/**
 *  global-warming-potential controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::global-warming-potential.global-warming-potential');
